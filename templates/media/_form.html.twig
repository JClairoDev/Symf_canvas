


{{ form_start(form, {'attr': {'class': 'form-group'}}) }}
    {{ form_row(form.photo, {'attr':{'class': 'form-control width-form-box'}}) }}
    {{ form_row(form.video, {'attr':{'class': 'form-control width-form-box'}}) }}
    {{ form_row(form.content, {'attr':{'class': 'form-control width-form-box'}}) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

